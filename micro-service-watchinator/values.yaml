resourceLimits:
  cpu: 100m
  memory: 128Mi

cronjob:
  # The websocket connect URI for the service being watchinated
  # example: wss://foo.bar.com/websocket
  consumerUri:
  image:
  schedule: "*/1 * * * *"

namespace: watchinator
